import { PresetShowcaseScreen } from '@/components';

export default function Screen() {
  return (
    <PresetShowcaseScreen
      title="Preset: collapse"
      subtitle="Content clips under the pinned title"
      preset="collapse"
      contentBackgroundColor="#E3CBFC"
    />
  );
}
